# Core data processing
pyspark==3.5.0
pandas==2.2.2
pyarrow==15.0.2

# AWS
boto3==1.34.0
s3fs==2024.3.1

# Streaming — kafka-python-ng is the maintained Python 3.12-compatible fork
kafka-python-ng==2.2.3

# Geospatial
geopandas==0.14.4
shapely==2.0.4

# Analytics / querying
duckdb==0.10.3

# Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.22.0

# Interactive dashboard
streamlit>=1.35.0

# NOTE: Airflow runs in Docker (see docker-compose.yml).
# It is NOT installed locally — Airflow 2.x requires Python <3.12
# and has hundreds of heavy transitive dependencies.

# Validation
# HTTP
requests==2.31.0

# Config
PyYAML==6.0.1
python-dotenv==1.0.0

# Testing
pytest==8.1.1
pytest-mock==3.14.0
moto[s3]==5.0.3
